---
import Layout from '../layouts/Layout.astro';
import Card from '../components/Card.astro';
import Header from '../components/Header.astro';
import Footer from '../components/Footer.astro';
import projectdata from '../_data/projects.astro';

export const meta = {
  title: 'Projects',
  description: 'A collection of projects I have worked on.',
};
/** @type {import('@astrojs
 * /types').LoadParams} */
export async function load() {
  return {
    projectdata,
  };
}



// check which project is being viewed and get the data for it ( get project from the url )
const project = projectdata.projects.find((project: { slug: any; }) => project.slug === params.slug);



---

