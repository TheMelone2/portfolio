<script src="https://kit.fontawesome.com/cae7f6ee03.js" crossorigin="anonymous"></script>
<div class="nav">
    <nav class="navlinks left">
        <a href="/#aboutme">About me</a>
        <a href="/#projects">Projects</a>
        <a href="/resume">Resume</a>
    </nav>
    <nav class="navlinks right">
        <a href="https://github.com/TheMelone2/portfolio" target="_blank" id="nav-github"><i class="fab fa-github"></i> Visit on GitHub</a>
        <a href="mailto:melon@easysystems.live" id="nav-contact"><i class="fa fa-paper-plane"></i> Get in Touch</a>       
    </nav>
</div>
<style>
    @import url('https://fonts.googleapis.com/css2?family=Roboto:wght@300&display=swap');

    * {
        font-family: 'Montserrat', sans-serif;
        scroll-behavior: smooth;
    }
    .nav {
        background-color: #333333a9;
        color: #fff;
        display: flex;
        flex-direction: row;
        position: fixed;
        top: 0;
        left: 0;
        right: 0;
        z-index: 100;
        height: 4em;
        align-items: center;
        justify-content: space-between;
        animation: fadeIn 0.7s ease-in-out;
        backdrop-filter: blur(10px);
    }

    .navlinks {
        display: flex;
        flex-direction: row;
        align-items: center;
        justify-content: space-between;
    }
    .navlinks a {
        color: #fff;
        text-decoration: none;
        margin: 0 0.5em;
    }
    .navlinks.left a {
        margin-right: 0.5em;
    }
    .navlinks.right a {
        margin-left: 0.5em;
    }

    .navlinks.left {
        font-family: 'Titillium Web';
        text-transform: uppercase;
        text-align: center;
        font-weight: 600;
    }

    .navlinks.left * {
        box-sizing: border-box;
        transition: all .35s ease;
    }

    .navlinks.left a {
        padding: .5em .8em;
        color: rgba(255,255,255,.5);
        position: relative;
        text-decoration: none;
        font-size: 20px;
    }

    .navlinks.left a::before,
    .navlinks.left a::after {
        content: '';
        height: 14px;
        width: 14px;
        position: absolute;
        transition: all .35s ease;
        opacity: 0;
    }

    .navlinks.left a::before {
        content: '';
        right: 0;
        top: 0;
        border-top: 3px solid #01ff6b7a;
        border-right: 3px solid #01FF6B;
        transform: translate(-100%, 50%);
    }

    .navlinks.left a:after {
        content: '';
        left: 0;
        bottom: 0;
        border-bottom: 3px solid #01ff6b7a;
        border-left: 3px solid #01FF6B;
        transform: translate(100%, -50%)
    }

    .navlinks.left a:hover:before,
    .navlinks.left a:hover:after{
        transform: translate(0,0);
        opacity: 1;
    }

    .navlinks.left a:hover {
        color: #01FF6B;
    }
    #nav-github {
        background-color: #ffffffe0;
        padding: 0.5em 1em;
        border-radius: 0.5em;
        color: #000;
        text-decoration: none;
        font-weight: bold;
        margin-right: 0.5em;
    }
    #nav-github:hover {
        background-color: #fff;
        color: #000;
        background: linear-gradient(90deg, rgb(255, 255, 255) 0%, rgba(255, 255, 255, 0.377) 100%);
        transition: all ease-in-out 0.2s;
        background-size: 200% 200%;
        animation: gradient 3s ease infinite;
    }
    #nav-contact {
        background-color: #01FF6B;
        padding: 0.5em 1em;
        border-radius: 0.5em;
        color: #000;
        text-decoration: none;
        font-weight: bold;
    }

    #nav-contact:hover{
        background-color: #fff;
        color: #000;
        transition: all ease-in-out 0.2s;
        background: linear-gradient(90deg, rgb(80, 255, 153) 0%, rgba(1, 255, 43, 0.596) 100%);
        background-size: 200% 200%;
        animation: gradient 3s ease infinite;
    }

    /* Mobile respomsive */
    @media only screen and (max-width: 600px) {
        .nav {
            height: 4em;
        }
        .navlinks.left a {
            font-size: 10px;
        }
        .navlinks.right a {
            font-size: 10px;
            height: 1.8em;
            width: 5em;
        }
    }
</style>
